---
description: Commenting standards — require Chinese intent comments for complex logic and public APIs
alwaysApply: true
---

# 注释规范

## 核心原则

- 注释用中文，代码用英文（变量名、函数名、类名）
- 注释应解释「为什么」，不要逐行翻译「做了什么」
- 代码调整后，相关注释必须同步更新，禁止注释与实现不一致

## 必须写注释的场景

- 复杂业务分支（多层条件、状态机、兜底降级）
- 权限控制与安全相关逻辑（鉴权、路由拦截、数据脱敏）
- 边界条件与兼容处理（空值、异常分支、平台差异）
- 性能或时序敏感逻辑（防抖节流、缓存失效、并发控制）

## 公共 API 与导出能力

- 对外导出的函数、composable、工具方法，需有简短注释说明用途
- 至少说明：输入约束、返回值语义、副作用（如有）

## 禁止的注释写法

- 无信息量注释（如：`// 给变量赋值`、`// 循环数组`）
- 与代码明显重复的注释（代码一眼可知的语句不应重复解释）
- 过期注释（逻辑已变更但注释未更新）

## 示例

```ts
// ❌ 无效注释：只是在翻译代码
// 如果是管理员
if (user.role === 'admin') {
  return true
}

// ✅ 有效注释：说明业务意图和约束
// 管理员账户跳过二次校验：用于应急处理流程，后续由审计日志补偿追踪
if (user.role === 'admin') {
  return true
}
```
