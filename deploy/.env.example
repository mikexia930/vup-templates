# ============================================
# 生产环境配置文件
# 适用于 DAU ≤ 5000 的项目
# ============================================
# 
# 使用方法：
# 1. 复制此文件为 .env: cp env.example .env
# 2. 根据实际情况修改各环境变量的值
# 3. 确保 .env 文件不被提交到版本控制系统
#
# ============================================

# ============================================
# 数据库配置 (MariaDB)
# ============================================
# 数据库名称
MYSQL_DATABASE=

# 数据库用户名
MYSQL_USER=

# 数据库用户密码（请使用强密码）
MYSQL_PASSWORD=

# 数据库 root 密码（请使用强密码）
MYSQL_ROOT_PASSWORD=

# ============================================
# API 服务配置
# ============================================
# CORS 允许的源（多个用逗号分隔）
# 例如: http://localhost:3000,https://yourdomain.com
CORS_ORIGIN=

# JWT 密钥（请使用随机生成的强密钥，至少 32 位）
# 生成方式: openssl rand -base64 32
JWT_SECRET=

# JWT Token 过期时间（默认: 7d）
JWT_EXPIRES_IN=7d

# JWT Refresh Token 过期时间（默认: 30d）
JWT_REFRESH_EXPIRES_IN=30d

# 文件上传最大大小（字节，默认: 1GB = 1073741824）
# 1MB = 1048576
# 10MB = 10485760
# 100MB = 104857600
# 1GB = 1073741824
UPLOAD_MAX_FILE_SIZE=1073741824

# 文件上传基础 URL（用于生成文件访问链接）
# 例如: https://yourdomain.com/uploads
# 或: http://localhost:9310/uploads
UPLOAD_BASE_URL=

# ============================================
# Admin 前端配置
# ============================================
# API 服务的基础 URL
# 例如: https://yourdomain.com/api
# 或: http://localhost:9310/api
API_BASE_URL=
# Admin 的路径
# 例如 /admin/ 
APP_BASE_URL=

# ============================================
# Nginx 端口配置
# ============================================
# HTTP 端口（默认: 80）
HTTP_PORT=80

# HTTPS 端口（默认: 443）
HTTPS_PORT=443

# ============================================
# 注意事项
# ============================================
# 1. 所有密码和密钥必须使用强密码
# 2. 生产环境请使用 HTTPS
# 3. 定期更换 JWT_SECRET
# 4. 确保数据库密码足够复杂
# 5. CORS_ORIGIN 只配置允许的域名
# 6. 文件上传大小根据实际需求调整

