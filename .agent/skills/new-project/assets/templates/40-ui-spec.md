<!--
AI 必读：

1. 用户必须提供 UI 素材（设计稿 / 截图 / 参考产品 / 文字描述），AI 严禁自己"想象"
2. 文字描述也要追问到具体（按钮文案、表单字段、空状态、错误提示）
3. 页面命名（路由 path + 中英文）必须用户确认
4. 本文档只列"做什么页 + 关键交互"，详细视觉设计走设计稿
-->

# UI / 交互稿

> 项目：<...> · 创建：<YYYY-MM-DD>

## 1. 设计稿来源

- **类型**：<Figma / 截图 / 参考产品 / 纯文字描述>
- **链接 / 位置**：<...>
- **本地文件**：设计稿截图 / 导出图统一放 `docs/designs/`（按页面命名，如
  `login.png`、`user-list.png`）

## 2. 页面清单

| 路由 path | 页面名   | 所属模块 | 关键功能             |
| --------- | -------- | -------- | -------------------- |
| `/login`  | 登录     | auth     | 账号密码登录         |
| `/users`  | 用户列表 | user     | 搜索 / 分页 / 增删改 |
| <...>     | <...>    | <...>    | <...>                |

## 3. 关键交互

仅列出**非常规**或**易出歧义**的交互（标准 CRUD 不必详写）：

- **<场景名>**：<用户操作 → 系统反馈>
  - 例：批量删除点击后弹二次确认，确认中按钮 loading

## 4. 空 / 错 / 加载状态

- **空状态**：<例：列表无数据时显示"暂无数据" + 引导新增按钮>
- **加载**：<例：表格用骨架屏，按钮用 spinner>
- **错误**：<例：接口失败用 toast，表单校验用字段下方红字>
