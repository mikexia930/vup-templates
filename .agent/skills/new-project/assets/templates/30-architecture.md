<!--
AI 必读：

1. 模块划分必须基于需求文档的功能列表，禁止"按经验"自定
2. 模块边界不清 → 停下来与用户讨论归属
3. 模块命名（中英对照）必须用户确认
4. 模块自己的可复用能力通过 modules/<name>/index.ts 暴露；common/ 只放真正全局通用能力
-->

# 架构设计

> 项目：<...> · 创建：<YYYY-MM-DD>

## 1. 模块划分

| 模块（英文） | 中文名     | 职责           | 主要功能             |
| ------------ | ---------- | -------------- | -------------------- |
| <user>       | <用户管理> | <用户增删改查> | <列表 / 详情 / 编辑> |
| <...>        | <...>      | <...>          | <...>                |

## 2. 数据流

<简述请求/响应链路。1-3 句话或一个简图>

```
用户 → 浏览器 → Vue SPA → 后端 API → DB
                            ↘ extends/payment-py
```

## 3. 目录结构

仅列出与基座默认不同 / 关键自定义部分：

```
apps/my-app/src/
├── modules/
│   ├── user/        # 用户模块
│   └── order/       # 订单模块
└── common/          # 全局通用能力（不属于某个业务模块）
```

## 4. 关键技术决策

仅记录与基座默认不同的决策，决策详情同步到 90-decision-log.md：

- <例：HTTP 客户端用 ofetch 而非 axios（理由：SSR 需求）>
